Read and follow `AGENTS.md` at the repository root before doing anything. It is the single source of agent instructions for this project. Skills are available under `.github/skills/` (installed from `skills/` by `aix install`).

