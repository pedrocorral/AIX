# security/ — the kit's own register and audit evidence
Read this when: touching downloads (`extern.py`, `source.py`), shell profiles (`selfinstall.py`), git pulls. Skip when: editing a skill's text.

| Path | What | Read when |
|---|---|---|
| `vulnerability-register.md` | `VUL-*` rows for the kit's attack surface | Any change to the files above |
| `audits/` | Audit reports (evidence for status changes) | Verifying a status |
| `threat-model.md` | Assets, actors, trust boundaries of a CLI that writes into repositories and home folders | Threat modelling |
