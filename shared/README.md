Contracts shared by both sides: OpenAPI/JSON schemas from `docs/requirements/api/`, `error-codes.*`, event schemas.
Allowed deps: none. Forbidden: runtime logic; imports from `backend/` or `frontend/`.
