# Trackers
- GitHub: `gh label create`, issues linked by `TASK-nnnn` in the body; PRs reference the task.
- GitLab: `glab label create`; same convention.
- Local: no external tracker; `docs/road-map/` is the whole truth; `aix task` is the CLI.
