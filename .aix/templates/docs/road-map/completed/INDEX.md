# road-map/completed/ — done tasks, by month, immutable
Read this when: asking whether something was done (`grep -rl "FR-…" completed/`). Skip when: picking new work.

| Path | What | Read when |
|---|---|---|
| `YYYY-MM/` | One folder per month, `INDEX.md` listing the tasks completed that month | History |
