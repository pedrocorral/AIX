# data-model/ — DM-<Entity> aggregates
Read this when: persistence, API schemas, migrations. Skip when: UI-only work.

| Path | What | Read when |
|---|---|---|
| `field-dictionary.md` | **Canonical field names** — the only names allowed in DM/API/schemas/code | Before naming any field, anywhere |
| `DM-Item.md` | Sample aggregate (delete with the example domain) | Example |
