# stacks/java/ — Java (Spring Boot) mapping
Read this when: the project is Java. Skip when: it is not.

| Path | What | Read when |
|---|---|---|
| `spring-boot.md` | Layout mapping, JPA as adapter, testing tools | Java projects |
