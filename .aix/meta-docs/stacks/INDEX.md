# stacks/ — mapping the language-agnostic architecture onto concrete stacks
Read this when: the stack is chosen (see `docs/requirements/decisions/ADR-0001-*`). Skip when: designing at the architecture level.

| Path | What | Read when |
|---|---|---|
| `python/INDEX.md` | Python web apps (FastAPI/Flask/Django), data-science apps, AI/LLM apps, tooling | Python projects |
| `java/INDEX.md` | Spring Boot mapping of the layout, JPA guidance | Java projects |
| `javascript/INDEX.md` | Frontend (React/Vue/Svelte) and Node backends mapping | JS/TS projects |

Adding a stack: copy `python/INDEX.md` structure; keep the same H2 headings so agents can jump by heading.
