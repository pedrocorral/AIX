Backend. Layout: `docs/meta-docs/architecture/project-layout.md`; stack: `docs/meta-docs/stacks/<lang>/`.
Allowed deps: `shared/`. Forbidden: importing `frontend/`; ORM/driver imports outside `app/*/adapters/` and `migrations/`.
